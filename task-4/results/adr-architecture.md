# ADR-0001: Реализация ETL-операций

## Контекст

Сотрудники складов в конце рабочей смены загружают csv-отчеты об остатках. При штатной нагрузке в момент обработки загруженных отчетов система работает стабильно. Но в периоды пиковых нагрузок производительность системы значительно деградирует.

Это провоцирует финансовые и репутационные риски, так как:

- Существенно падает операционная эффективность сотрудников.
- Она приводит к просрочкам поставок и неэффективной логистике.
- На уровне клиентов из-за этого возникают цепные задержки в строительных процессах.

В ближайшее время ожидается существенный рост и базовой, и пиковой нагрузок. Увеличится:

- число пользователей
- число заявок на хранение
- объемов данных по движениям

Также:

- расширится номенклатура товаров
- потребуется интеграция с новыми источниками данных

## Технический анализ

Деградацию производительности при росте нагрузки вызывают:

- Отсутствие изоляции ресурсов:
  - Отчеты обрабатывает монолит
    - Процессы обработки конкурируют за CPU и RAM с основными бизнес-функциями и занимют общий пул потоков.
- Неэффективная работа с БД:
  - Данные вставляются построчно. Каждая из тысяч строк инициирует отдельную транзакцию.
    - Это перегружает транзакционный лог PostgreSQL и блокирует таблицы, делая базу недоступной для оперативных запросов.

## Проблема

Разработать архитектуру решения для ETL-операций и хранения данных, которая удовлетворяет следующим требованиям:

### Use cases

1.  UQ-001: Валидация отчета

    - Действующие лица или системы:
      - Сотрудник склада
      - Система
    - Описание:
      - Сотрудник загружает CSV-отчет
      - Система валидирует данные
      - Если найдены ошибки
        - то система отказывает в обработке отчета и демонстрирует информацию о несоотвествиях.
        - иначе система подтверждает принятие отчета для обработки.

2.  UQ-002: Пакетная обработка

    - Действующие лица или системы:
      - Система
      - БД
    - Описание:
      - система проводит глубокую валидацию в соответствии с бизнес-логикой, обогащает данные и сохраняет их в БД пачками (chunks).
      - Если во время записи произошел технический сбой (например, БД временно недоступна)
        - то система повторяет попытку несколько раз без дулирования работы.
      - По истечению попыток система прерывает обработку.
      - Администратор может перезапустить задачу с момента прерывания без дублирования данных.

3.  UQ-003: Мониторинг

    - Действующие лица или системы:
      - Система
      - Администратор
    - Описание:
      - Система предоставляет отчет о результате:
        - сколько строк успешно загружено,
        - сколько отклонено из-за ошибок в данных, и
        - сколько времени занял процесс.
      - Если в процессе обработки произошли ошибки
        - то Система отпавляет Администратору уведомление.

### Нефункциональные

1.  Производительность

    1.  REQ-NF-001: Обработка 2000 строк — до 30 секунд.

        Среднее время обработки отчёта на 2000 строк составляет до 30 секунд.

2.  Масштабируемость

    1.  REQ-NF-002: Поддержка 150 параллельных загрузок в пиковые часы без влияния на ERP

        Обрабатывать пиковые нагрузки до 150 параллельных загрузок в пиковые часы без деградации времени отклика основной ERP.

    2.  REQ-NF-003: Гибкая масштабируемость в зависимости от нагрузки

        Возможность гибко масштабироваться под нагрузкой.

3.  Инфраструктура

    1.  REQ-NF-004: Готовность к миграции в GCP

        Готовность к контейнеризации (Docker) и миграции в GCP.

4.  Архитектура

    1.  REQ-NF-005: Изолированность от монолита

        Вынести обработку отчетов за пределы монолита

    2.  REQ-NF-007: Модульность пайплайна

        Архитектура должна позволять добавлять новые этапы обработки или менять источники данных без переписывания всего ядра системы. Использование декларативного описания Job для быстрой адаптации под новую номенклатуру.

5.  Надежность

    1.  REQ-NF-006: Устойчивость этапов обработки к краткосрочным сбоям

        Система должна поддерживать настраиваемые политики повторных попыток (Retry). В том числе при интеграции с внешними источниками.

    2.  REQ-NF-008: Идемпотентность обработки

        Повторные запуски одного и того же отчета или его частей (после Retry) не должны приводить к дублированию записей или искажению остатков в БД.

## Решение

Схемы

- C2.puml
- C3 dataflow-short.puml

Spring Batch - выбран для реализации логики обработки данных. PostgreSQL - выбран для хранения промежуточных состояний Spring Batch (JobRepository).

### Аргументация

1.  ✅ Выбор Spring Batch

    - Обоснован <span class="spurious-link" target="adr-0002">*adr-0002*</span>

2.  ✅ Выбор PostgreSql для Spring Batch JobRepository

    - оптимален, так как:
      - закрывает требования по надежности
      - соответствует текущему стеку
      - соответствует облачной инфраструктуре

3.  ✅ [id:uq-001](id:uq-001)

    - **Элемент схемы:**
      - Монолит
    - **Механизм реализации:**
      - Gatekeeper Pattern
    - **Доказательство**
      - Делает первичную проверку формата, не пуская «мусор» в S3 и Воркер.

4.  ✅ [id:uq-002](id:uq-002)

    - **Элемент схемы:**
      - Воркер и его связи
    - **Механизм реализации:**
      - Step Persistence: retries, restartability
      - Performance: chunk processing, records skipping
      - Logic complexity: modular declaration of ETL steps
    - **Доказательство**
      - описание процесса глубокой валидации, обогащения и записи реализуется встроенными средствами Spring Batch

5.  ✅ [id:uq-003](id:uq-003) Мониторинг

    - **Элемент схемы:**
      - Обратная связь Воркер-Монолит
    - **Механизм реализации:**
      - Message queue
    - **Доказательство**
      - Монолит подписан на сообщения о событиях обработки от воркера.
      - Монолит обновляет статус обработки отчета и использует текущие средства оповещения пользователей

6.  ✅ [id:req-nf-001](id:req-nf-001)

    - **Элемент схемы:**
      - Воркер
    - **Механизм реализации:**
      - Сhunk processing
    - **Доказательство**
      - Воркер загржуает, обрабатывает и записывает данные пакетно средствами Spring Batch в сервисе, отдельном от монолита.

7.  ✅ [id:req-nf-002](id:req-nf-002)

    - **Элемент схемы:**
      - Монолит, GCP Pub/Sub, Воркер
    - **Механизм реализации:**
      - Message queue, horizontal scaling, isolation
    - **Доказательство**
      - задача монолита - только переложить файлы отчетов в хранилище. Нагрузка на него минимальна.
      - Воркер вынесен в отдельный сервис и ему выдаются независимые ресурсы для обеспечения производительности.
      - Добавление поднятых инстансов воркеров позволяет распределить поток отчетов для сохранения производительности под нагрузкой.
      - Монолит стреляет задачами на обработку через очередь
      - Монолит получает обратную связь о событиях в процессе обработки через очередь

8.  ✅ [id:req-nf-003](id:req-nf-003)

    - **Элемент схемы:**
      - Монолит, GCP Pub/Sub, Воркер
    - **Механизм реализации:**
      - Google Kubernetes Engine (GKE), GCP Pub/Sub, HPA (Horizontal Pod Autoscaler)
    - **Доказательство**
      - в Google Cloud Platform можно настроить автоматическое масштабирование по количеству сообщений в очереди

9.  ✅ [id:req-nf-004](id:req-nf-004)

    - **Элемент схемы:**
      - Воркер
    - **Механизм реализации:**
      - Docker
    - **Доказательство**
      - Является самостоятельным Java приложением, которое можно упаковать в Docker

10. ✅ [id:req-nf-006](id:req-nf-006)

    - Реализуется средствами Spring Batch

11. ✅ [id:req-nf-007](id:req-nf-007)

    - Реализуется средствами Spring Batch

12. ✅ [id:req-nf-008](id:req-nf-008)

    - Частично реализуется средствами Spring Batch savepoints.


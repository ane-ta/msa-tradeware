apiVersion: 1

# источник файлов, лежит по такому-то пути, обновляй его раз в 5 минут».
providers:
  - name: 'Prometheus'
    orgId: 1
    folder: ''
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /etc/grafana/provisioning/dashboards